## 파일
*   `main.py`: 프로젝트를 실행하는 메인 파일
*   `utils.py`: 메인 스크립트에서 사용되는 유틸리티 함수 (크롤링, 작업시간 예측, 선석계획)
*   `lgbm_weight.pkl`: LightGBM 모델 가중치
*   `ship_info.csv`: 선박 길이 데이터 및 총톤수가 포함된 CSV 파일
*   `gurobi.lic`: Gurobi의 라이선스 파일

## 실행
```sh
$ python main.py